---
layout: '../../layouts/Layout.astro'
title: 'Intro'
---
import {components} from '../../components/MDX';
export {components};

import { Hello } from '../../components/Hello';

# {frontmatter.title}

# H1

## H2

### H3

#### H4

##### H5

###### H6

Paragraph.

Paragraph with `file.txt` mention.

Paragraph with [relative link](/).

Paragraph with [absolute link](https://github.com/satelllte/wgsl-canvas).

### Image

<img alt="UV fragment shader" src="/assets/uv.png" width="250" height="250" />

### Code: TypeScript

```ts
/////////////////////
// Code: TypeScript
/////////////////////

import { WGSLCanvas } from "@wgsl-canvas/core";

// 1. Check WebGPU support
if (!WGSLCanvas.isSupported()) {
  alert("WebGPU is not supported in this browser");
  return;
}

// 2. Create a canvas
const canvas = document.createElement("canvas");
canvas.width = 500;
canvas.height = 500;
document.body.appendChild(canvas);

// 3. Create an instance of "WGSLCanvas" and initialize it
const wgslCanvas = new WGSLCanvas({ canvas });
await wgslCanvas.init();

// 4. Make your first render
wgslCanvas.render();
```

### Code: WGSL

```wgsl
/////////////////////
// Code: WGSL
/////////////////////

struct FragmentOutput {
  @location(0) color: vec4<f32>,
}

@fragment
fn fragment_main() -> FragmentOutput {
  var output: FragmentOutput;
  output.color = vec4<f32>(0.1, 0.2, 0.25, 1.0);
  return output;
}
```

### React component

<Hello client:load />
