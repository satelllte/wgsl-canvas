---
interface Props {
  name: string;
}

import Layout from './Layout.astro';
import { Example } from '../components/Example';

const { name } = Astro.props;
---

<Layout>
  <Example name={name}/>
  <slot/>
</Layout>
